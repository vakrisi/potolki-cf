#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ОбработкаСобытий

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	ОбщийМодуль.ЗаполнитьШапкуДокумента(ЭтотОбъект);
	
	ТипДанныхЗаполнения = ТипЗнч(ДанныеЗаполнения);
	//Если ТипДанныхЗаполнения = Тип("ДокументСсылка.Замер") Тогда
	//	Основание = ДанныеЗаполнения.Ссылка;
	//	
	//Иначе
	Если ТипДанныхЗаполнения = Тип("Структура") Тогда
		ЗаполнитьЗначенияСвойств(ЭтотОбъект, ДанныеЗаполнения);
	
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриКопировании() 
	
	Отправлен = Ложь;
	ДатаОтправки = Дата(1,1,1);
	
	//++ №8201 очищаем признаки отправки в ТЧ
	Для Каждого ТекущаяСтрока Из Получатели Цикл
		ТекущаяСтрока.Отправлено = Ложь;
		ТекущаяСтрока.ДатаОтправки = Дата(1,1,1);
	КонецЦикла;
	//-- №8201
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	//++ №8201 закомментировано, т.к. он отрабатывал только в определенных случаях
	//Если Получатели.Количество() = 0 Тогда
	//	НовыйПолучатель = Получатели.Добавить();
	//	НовыйПолучатель.Email = АдресПолучателя;
	//	НовыйПолучатель.Контрагент = Контрагент;
	//КонецЕсли;
	//-- №8201
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли