
Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	// регистр СтоимостьВыработкиНоменклатуры
	Движения.СтоимостьВыработкиНоменклатуры.Записывать = Истина;
	Для Каждого ТекСтрокаСтоимостьВыработки Из СтоимостьВыработки Цикл
		Движение = Движения.СтоимостьВыработкиНоменклатуры.Добавить();
		Движение.Период = Дата;
		Движение.ВидБригады = ВидБригады;
		Движение.Номенклатура = ТекСтрокаСтоимостьВыработки.Номенклатура;
		Движение.Стоимость = ТекСтрокаСтоимостьВыработки.Стоимость;
	КонецЦикла;
	
	Движения.СтоимостьВыработкиПотолкаРозница.Записывать = Истина;
	Если УстановитьСтоимостьВыработкиПотолка Тогда
		Движение = Движения.СтоимостьВыработкиПотолкаРозница.Добавить();
		Движение.Период = Дата;
		Движение.ВидБригады = ВидБригады;
		Движение.Стоимость = СтоимостьВыработкиПотолка;
	КонецЕсли;

	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры
