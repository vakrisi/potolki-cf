Процедура ОбработкаПроведения(Отказ, Режим)
	
	//проверим, был ли введен по данному году и менеджеру документ ранее
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ПланированиеПродажТехнологовЗамера.Ссылка
	|ИЗ
	|	Документ.ПланированиеПродажТехнологовЗамера КАК ПланированиеПродажТехнологовЗамера
	|ГДЕ
	|	ПланированиеПродажТехнологовЗамера.Год = &Год
	|	И ПланированиеПродажТехнологовЗамера.Проведен = ИСТИНА
	|	И ПланированиеПродажТехнологовЗамера.Ссылка <> &Ссылка
	|	И ПланированиеПродажТехнологовЗамера.Технолог = &Технолог";
	Запрос.УстановитьПараметр("Год",Год);
	Запрос.УстановитьПараметр("Технолог",Технолог);
	Запрос.УстановитьПараметр("Ссылка",Ссылка);
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	Пока ВыборкаЗапроса.Следующий() Цикл
		Отказ = Истина;
		Сообщить("По данному году и технологу уже имеется проведенный документ плана: " + ВыборкаЗапроса.Ссылка,СтатусСообщения.Важное);
	КонецЦикла;
	Если Отказ Тогда 
		Возврат;
	КонецЕсли;
	
	
	Движения.ПланПродажТехнологов.Записывать = Истина;
	Для Каждого СтрокаПлан Из План Цикл
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 1;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Январь;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 2;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Февраль;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 3;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Март;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 4;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Апрель;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 5;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Май;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 6;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Июнь;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 7;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Июль;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 8;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Август;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 9;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Сентябрь;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 10;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Октябрь;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 11;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Ноябрь;
		
		Движение = Движения.ПланПродажТехнологов.Добавить();
		НомерМесяц = 12;
		Движение.Технолог = Технолог;
		Движение.Год = Год;
		Движение.Месяц = НачалоМесяца(Дата(Год,НомерМесяц,1,0,0,1));
		Движение.Номенклатура = СтрокаПлан.Номенклатура;
		Движение.Количество = СтрокаПлан.Декабрь;
		
	КонецЦикла;

	
КонецПроцедуры