
&НаКлиенте
Процедура ВыполненоПриИзменении(Элемент)
	Если Запись.Выполнено Тогда
		Запись.ДатаВыполнения = ТекущаяДата();
	Иначе
		Запись.ДатаВыполнения = Дата(1,1,1);
	КонецЕсли;
КонецПроцедуры
