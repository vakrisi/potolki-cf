
&НаСервере
Процедура ОчиститьЛогНаСервере()
	НаборЗаписей = РегистрыСведений.ЛогРОЙСТАТ.СоздатьНаборЗаписей();
	НаборЗаписей.Записать();
	Элементы.Список.Обновить();
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьЛог(Команда)
	ОчиститьЛогНаСервере();
КонецПроцедуры
