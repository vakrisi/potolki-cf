
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если НЕ РольДоступна("АдминистраторСистемы") Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	// пока закомментировано, невозможно редактировать запись через форму
	//Если ЭтотОбъект.Количество() = 0 Тогда
	//    //это пустой набор записей
	//	Если НЕ ДополнительныеСвойства.Свойство("УдалениеНеиспользуемых") Тогда 
	//		Отказ = Истина;
	//	КонецЕсли;        
	//КонецЕсли; 
	
КонецПроцедуры
