Функция CheckCeilingGET(Запрос)
	
	Если Не Константы.ИспользуетсяСервисПроверкиПотолков.Получить() Тогда
		Возврат Новый HTTPСервисОтвет(400);
	КонецЕсли;
	
	Ответ = Новый HTTPСервисОтвет(200);
	ИмяМетода = Запрос.ПараметрыURL.Получить("ИмяМетода");
	
	СтруктураОтвета = Новый Структура;
	
	//пришел запрос на неизвестный метод
	Ответ.КодСостояния = 405;
	СтруктураОтвета.Вставить("result", "Unknown request");
	
	//формируем jSON ответ
	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	
	ЗаписатьJSON(ЗаписьJSON, СтруктураОтвета);
	
	СтрокаДляОтвета = ЗаписьJSON.Закрыть();
	
	Ответ.Заголовки.Вставить("Content-Type","text/html; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(СтрокаДляОтвета,КодировкаТекста.UTF8);	
	
	Возврат Ответ;

КонецФункции

Функция CheckCeilingPOST(Запрос)
	
	Если Не Константы.ИспользуетсяСервисПроверкиПотолков.Получить() Тогда
		Возврат Новый HTTPСервисОтвет(400);
	КонецЕсли;
	
	Ответ = Новый HTTPСервисОтвет(200);
	ИмяМетода = Запрос.ПараметрыURL.Получить("ИмяМетода");
	
	СтруктураОтвета = Новый Структура;
	
	//ОБРАБОТКА ЗАПРОСОВ
	Если НРЕГ(ИмяМетода) = "checkceiling" Тогда
		
		ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
		Ответ.КодСостояния = 200;

		СтруктураОтвета.Вставить("number",МобильныйКлиент.ПолучитьИнформациюПоПотолку(ТелоЗапроса));
	Иначе
		//пришел запрос на неизвестный метод
		Ответ.КодСостояния = 405;
		СтруктураОтвета.Вставить("result", "Unknown request");
	КонецЕсли; 
	
	//формируем jSON ответ
	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	
	ЗаписатьJSON(ЗаписьJSON, СтруктураОтвета);
	
	СтрокаДляОтвета = ЗаписьJSON.Закрыть();
	
	Ответ.Заголовки.Вставить("Content-Type","text/html; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(СтрокаДляОтвета,КодировкаТекста.UTF8);	
	
	Возврат Ответ;
	
	
КонецФункции
