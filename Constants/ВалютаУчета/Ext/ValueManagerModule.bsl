
Процедура ПередЗаписью(Отказ)
	
	Если ЗначениеЗаполнено(Значение) И НЕ Значение.Используется Тогда
		ОбщегоНазначения.СообщитьПользователю("Для выбранной валюты учета не выставлен признак ""Используется""", , , , Отказ)		
	КонецЕсли;
	
КонецПроцедуры
