
&НаКлиенте
Процедура ИзменитьПорядок(Команда)
	
	Вверх = (Команда.Имя = "ПереместитьВверх");
	
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;	
	
	ИзменитьПорядокНаСервере(ТекущиеДанные.Ссылка, Вверх);
	
	Элементы.Список.Обновить();
	
КонецПроцедуры

Процедура ИзменитьПорядокНаСервере(ТекСтатус, Вверх)
	
	ОбщийМодульСервер.ИзменитьРеквизитДопУпорядочивания("СтатусыЗаказовПокупателей", ТекСтатус, Вверх);
	
	//Если Вверх Тогда
	//	ТекстЗапроса = 
	//	"ВЫБРАТЬ ПЕРВЫЕ 2
	//	|	СтатусыЗаказовПокупателей.Ссылка КАК Ссылка,
	//	|	СтатусыЗаказовПокупателей.РеквизитДопУпорядочивания КАК РеквизитДопУпорядочивания
	//	|ИЗ
	//	|	Справочник.СтатусыЗаказовПокупателей КАК СтатусыЗаказовПокупателей
	//	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.СтатусыЗаказовПокупателей КАК ТекущийСтатус
	//	|		ПО (ТекущийСтатус.Ссылка = &ТекущийСтатус)
	//	|ГДЕ
	//	|	СтатусыЗаказовПокупателей.РеквизитДопУпорядочивания <= ТекущийСтатус.РеквизитДопУпорядочивания
	//	|
	//	|УПОРЯДОЧИТЬ ПО
	//	|	СтатусыЗаказовПокупателей.РеквизитДопУпорядочивания УБЫВ";	
	//Иначе
	//	ТекстЗапроса = 
	//	"ВЫБРАТЬ ПЕРВЫЕ 2
	//	|	СтатусыЗаказовПокупателей.Ссылка КАК Ссылка,
	//	|	СтатусыЗаказовПокупателей.РеквизитДопУпорядочивания КАК РеквизитДопУпорядочивания
	//	|ИЗ
	//	|	Справочник.СтатусыЗаказовПокупателей КАК СтатусыЗаказовПокупателей
	//	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.СтатусыЗаказовПокупателей КАК ТекущийСтатус
	//	|		ПО (ТекущийСтатус.Ссылка = &ТекущийСтатус)
	//	|ГДЕ
	//	|	СтатусыЗаказовПокупателей.РеквизитДопУпорядочивания >= ТекущийСтатус.РеквизитДопУпорядочивания
	//	|
	//	|УПОРЯДОЧИТЬ ПО
	//	|	СтатусыЗаказовПокупателей.РеквизитДопУпорядочивания";
	//КонецЕсли;
	// 
	// 
	//Запрос = Новый Запрос;
	//Запрос.УстановитьПараметр("ТекущийСтатус", ТекСтатус);
	//Запрос.Текст = ТекстЗапроса;
	//
	//Выборка = Запрос.Выполнить().Выбрать();	
	//Если НЕ Выборка.Количество() = 2 Тогда
	//	Возврат;
	//КонецЕсли;
	//
	//Выборка.Следующий();
	//СпрОбъект1 = Выборка.Ссылка.ПолучитьОбъект();	
	//Порядок1 = Выборка.РеквизитДопУпорядочивания;
	//
	//Выборка.Следующий();
	//СпрОбъект2 = Выборка.Ссылка.ПолучитьОбъект();
	//Порядок2 = Выборка.РеквизитДопУпорядочивания;
	//
	//СпрОбъект1.РеквизитДопУпорядочивания = Порядок2;
	//СпрОбъект2.РеквизитДопУпорядочивания = Порядок1;
	//
	//Попытка
	//	СпрОбъект1.Записать();	
	//	СпрОбъект2.Записать();
	//Исключение
	//	Сообщить(ОписаниеОшибки());
	//КонецПопытки;
	
КонецПроцедуры
