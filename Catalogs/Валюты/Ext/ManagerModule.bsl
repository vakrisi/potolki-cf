///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2019, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются 
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.ГрупповоеИзменениеОбъектов

// Возвращает реквизиты объекта, которые разрешается редактировать
// с помощью обработки группового изменения реквизитов.
//
// Возвращаемое значение:
//  Массив - список имен реквизитов объекта.
Функция РеквизитыРедактируемыеВГрупповойОбработке() Экспорт
	
	Результат = Новый Массив;
	Результат.Добавить("СпособУстановкиКурса");
	Результат.Добавить("Наценка");
	Результат.Добавить("ОсновнаяВалюта");
	Результат.Добавить("ФормулаРасчетаКурса");
	Возврат Результат;
	
КонецФункции

// Конец СтандартныеПодсистемы.ГрупповоеИзменениеОбъектов

#КонецОбласти

#КонецОбласти

//++ Потолки
#Область ОбновлениеИнформационнойБазы

// Процедура заполняет справочник по умолчанию
//
Процедура ЗаполнитьПоставляемыеВалюты() Экспорт
	
	СпрОбъект = Справочники.Валюты.BYR.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Белорусский рубль";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.BYN.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Белорусский рубль";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.EUR.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Евро";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.KGS.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Сом";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.KZT.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Тенге";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.RUB.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Российский рубль";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.UAH.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Гривна";
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.Валюты.USD.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Доллар США";
	СпрОбъект.Записать();
	
	// #12474
	СпрОбъект = Справочники.Валюты.MDL.ПолучитьОбъект();
	СпрОбъект.НаименованиеПолное = "Молда́вский лей";
	СпрОбъект.Записать();
	
КонецПроцедуры

#КонецОбласти
//-- Потолки

#КонецЕсли
